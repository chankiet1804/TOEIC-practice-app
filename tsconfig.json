{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "strictNullChecks": true,
    "typeRoots": ["./types", "./node_modules/@types"],
  },
  "include": [
    "env.d.ts", "src/**/*.ts"  // hoặc đường dẫn tới file env.d.ts của bạn
  ]
}
